"# Render configuration deprecated"

"This repository has been migrated to Vercel. The original Render configuration has been archived and should no longer be used. See `vercel.json` for the new deployment settings."
          property: url
          path: /callback